# .env.example - LiveScratch backend
# Este archivo es solo de ejemplo. Render inyecta los valores reales.

# Puerto en el que corre el backend (Render lo asigna automáticamente)
PORT=${PORT}

# Usuarios administradores para rutas protegidas (JSON string)
# Ejemplo seguro: {"usuario1":"contraseña1"}
ADMIN_USER=${ADMIN_USER}

# Lista de usuarios administradores (solo Scratch usernames, JSON array)
# Ejemplo: ["usuario1","usuario2"]
ADMIN=${ADMIN}

# Proyectos permitidos (solo Scratch project IDs, JSON array)
# Para permitir todos, puedes poner []
AUTH_PROJECTS=${AUTH_PROJECTS}

# Webhook de Discord para notificaciones de chat
CHAT_WEBHOOK_URL=${CHAT_WEBHOOK_URL}
